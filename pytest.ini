[pytest]
testpaths = tests
# Put the project root on sys.path so `pytest` works the same as
# `python -m pytest` (without this, only tests/ is added and `import app` fails).
pythonpath = .
addopts = -q --disable-warnings
log_cli = false
log_level = WARNING
filterwarnings =
    ignore::DeprecationWarning
